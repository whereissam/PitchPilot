// @ts-check
import { defineConfig } from 'astro/config'

// Static site. Vercel auto-detects Astro and serves the dist/ output.
export default defineConfig({
  site: 'https://pitchpilot.vercel.app',
})
