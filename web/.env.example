# LiveKit Cloud credentials (same values as agent/.env).
# Read server-side by src/routes/api.token.ts; NOT VITE_-prefixed, so never sent to the browser.
LIVEKIT_URL=wss://YOUR-PROJECT.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
